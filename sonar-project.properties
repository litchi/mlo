# This script should be run after the unit test been completed
# required metadata
sonar.projectKey=BB10GTD
sonar.projectName=Mind Like Water
sonar.projectVersion=1.1

# optional description
sonar.projectDescription=Mind like water GTD App for BlackBerry 10 Platform, developed in HTML5+JS+Web SQL

# path to source directories (required), the exact file invoked by unit test script
sonar.sources=target/src/js/app

# path to test source directories (optional) the exact unit test file been invoked.
sonar.tests=target/qa/JsTestDriver/src 

# path to project binaries (optional), for example directory of Java bytecode
# sonar.binaries=

# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of classes are supported.
# sonar.libraries=path/to/library/*.jar,path/to/classes/dir,path/to/specific/library/myLibrary.jar,parent/*/*.jar

# The value of the property must be the key of the language.
sonar.language=js

sonar.javascript.testframework=jstestdriver

# Reuse report to analysis code coverage etc.
sonar.dynamicAnalysis=reuseReports
sonar.javascript.jstestdriver.reportsfolder=target/qa/JsTestDriver/report/xml
sonar.javascript.jstestdriver.coveragefile=jsTestDriver.conf-coverage.dat

# Additional parameters
# sonar.my.property=value
